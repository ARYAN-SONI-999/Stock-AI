# SECURITY

Security baseline:
- Environment-variable based secret management.
- CORS policy configuration in API settings.
- No provider/API keys exposed in frontend source.
- Explicit delayed/unavailable responses rather than fabricated data.

Next phases include JWT auth hardening, RBAC, audit logs, and stricter input/rate controls.
